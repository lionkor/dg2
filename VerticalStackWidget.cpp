#include "VerticalStackWidget.h"

VerticalStackWidget::VerticalStackWidget(Widget* parent, class WidgetConstraints* constraints, const sf::Color& color)
    : StackWidget(parent, constraints, color)
{
}
