#include "HorizontalStackWidget.h"

HorizontalStackWidget::HorizontalStackWidget(Widget* parent, class WidgetConstraints* constraints, const sf::Color& color)
    : StackWidget(parent, constraints, color)
{
}


